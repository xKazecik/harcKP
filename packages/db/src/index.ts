/**
 * Punkt wejścia pakietu @harc/db — reeksport klienta Prisma.
 *
 * @remarks Pakiet jest jedynym miejscem, przez które aplikacje sięgają do bazy.
 */
export { PrismaClient, Prisma } from '@prisma/client';
export * from '@prisma/client';
